#!/bin/bash
node -e "require('@pengtaohenry1213-prog/coding-standards/src/install.js').autoSync()"

git checkout HEAD -- .cursor/
if [ -f "package.json" ]; then
	npm run sync-standards 2>/dev/null || true
fi
echo "✅ 已自动同步团队统一 Cursor 配置"
exit 0